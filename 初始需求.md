# 坦克大战（Tank Battle）游戏需求文档

**版本**：2.0
**目标平台**：PC（Windows / macOS / Linux，开发与调试以 4K 显示器为基准）
**技术栈建议**：Python 3.9+ + Pygame 2.x
**交付物**：可直接运行的 Python 程序

> **版本 2.0 主要变更**：针对 4K 显示器调整了地图 / Tile / 坦克尺寸参数（见 2.2.1、2.3），并明确标注了资源可缺省（见第 5 章）。
> 阶段化开发计划见独立文档《开发阶段拆分.md》。

---

## 1. 项目概述

### 1.1 游戏简介
一款俯视角 2D 坦克射击游戏。玩家操控一辆坦克，在拥有丰富障碍物的大地图中与 AI 敌人交战。支持两种模式：PVE（1 玩家 vs N 个 AI）与混战（所有坦克互相攻击，最后存活者获胜）。地图中会随机刷新增益道具，提供加速、护盾、回血、特殊弹药等效果。游戏包含多个关卡，每关可配置不同地图和坦克外观。

### 1.2 核心玩法
- 玩家通过键盘控制坦克移动与射击。
- 坦克有生命值、移动速度、炮弹类型等属性。
- 障碍物分可破坏与不可破坏两类。
- 右上角小地图实时显示全局坦克与障碍物位置。
- 道具随机生成，拾取后立即生效或替换当前弹药。
- 关卡目标：PVE 模式消灭所有敌人；混战模式成为最后唯一幸存者。

### 1.3 用户提供资源（可全部缺省，不提供则用程序生成的几何图形代替，游戏不崩溃）
- 关卡地图图片（可选，若不提供则使用程序自动生成的地图）
- 我方坦克图片、敌方坦克图片（每种类型可不同）
- 道具图标、障碍物纹理等（若不提供则用几何图形代替）
- 背景音乐、音效文件（可选，若无则静音）

> 资源缺省策略详见第 5 章「美术与资源规格」。

---

## 2. 功能需求

### 2.1 游戏状态与流程
```
主菜单 → 模式选择 → 关卡选择 → 游戏进行中 → 暂停菜单 → 胜利/失败结算 → 返回主菜单
```
- **主菜单**：开始游戏、选择模式、选择关卡、退出。
- **模式选择**：PVE 模式、混战模式。
- **关卡选择**：列出所有可用关卡（可滚动），每个关卡显示预览图及名称。
- **游戏进行中**：玩家操控坦克，显示 HUD（血量、弹药类型、小地图、剩余敌人数量）。
- **暂停菜单**：按 ESC 键暂停，显示“继续”“重新开始”“返回主菜单”。
- **结算**：胜利/失败界面，显示用时、击杀数等，并提供“下一关”（PVE 模式）或“重新开始”。

### 2.2 战场地图与场景

#### 2.2.1 地图规格
- 地图尺寸：**6000×6000 像素**（可根据关卡调整）。此尺寸为 4K 视口的 3 倍以上，保证镜头无法一览全局、具备探索与滚动感。
- 镜头跟随玩家坦克，平滑滚动（玩家坦克保持在屏幕中心偏移 150 像素内）。
- 地图由 Tile（瓦片）构成，每个 Tile 尺寸 **64×64 像素**（4K 下视觉更清晰）。
- 障碍物类型：
  - **不可破坏**：砖墙、钢墙、水（坦克不能通过，炮弹不能穿过）。
  - **可破坏**：土墙、木箱（炮弹击中后消失）。
  - **草丛**：坦克可进入，半透明渲染，不影响移动和碰撞，但会隐藏坦克部分车身（视觉遮挡）。
- 地图边界为不可穿越的隐形墙壁。

#### 2.2.2 小地图（右上角）
- 位置：屏幕右上角，默认尺寸 **220×220 像素**（相对 4K 视口），半透明背景。
- 内容：
  - 缩略显示整个地图的障碍物（灰色方块）、玩家坦克（绿色亮点）、敌方坦克（红色亮点）、道具（黄色亮点）。
  - 小地图上仅显示坦克位置，不区分朝向。
  - 当前可视范围在小地图上用白色矩形框表示。
- 实现方式：将地图上所有对象按比例缩放绘制到固定 Surface 上。

#### 2.2.3 关卡地图配置
每个关卡对应一个 JSON 配置文件，包含：
```json
{
  "level_id": 1,
  "map_file": "assets/maps/level1.png",  // 可选，若提供则直接加载图片作为地图背景，障碍物仍需单独定义
  "tiles": "...." // 二维数组或字符串表示障碍物布局，若使用程序生成则不必手动填写
}
```
如果用户未提供地图图片，程序将根据 tile 数据自动绘制地图（不同障碍物用不同颜色/纹理表示）。

### 2.3 关卡设计

- 游戏预设 **10 个关卡**，难度递增。（首版可只实现 3 关，见《开发阶段拆分.md》）
- 每个关卡可配置：
  - 地图布局（障碍物密度、位置）。
  - 敌方坦克数量（3 ~ 10）。
  - 敌方坦克类型（普通、快速、重型等）。
  - 我方坦克外观（图片路径）、敌方每种类型的外观。
  - 道具刷新频率（秒）、最大道具数量、可用道具种类。
  - 敌方 AI 难度参数（反应时间、瞄准精度、躲避概率）。

- **坦克类型属性示例**（可在配置中修改；尺寸为依据 64×64 Tile 调整后的建议值）：
  | 类型   | 尺寸   | 生命值 | 移动速度(px/s) | 炮弹速度 | 射击间隔(ms) | 初始弹药 | 特点               |
  |--------|--------|--------|----------------|----------|--------------|----------|--------------------|
  | 玩家   | 48×48  | 100    | 200            | 400      | 800          | 普通弹   | 可拾取道具升级     |
  | 轻型   | 40×40  | 60     | 250            | 450      | 1000         | 普通弹   | 移动快，血少       |
  | 中型   | 44×44  | 100    | 180            | 380      | 1200         | 普通弹   | 均衡               |
  | 重型   | 52×52  | 150    | 120            | 350      | 1500         | 普通弹   | 血厚，慢，射击慢   |
  | Boss   | 64×64  | 300    | 150            | 500      | 1000         | 导弹     | 每关最后出现一个   |

### 2.4 战斗模式

#### 2.4.1 PVE 模式（玩家 vs AI）
- 玩家出生点固定（地图中央偏下）。
- AI 坦克在地图随机位置生成（避开玩家一定距离，且不在不可通行区域）。
- AI 坦克行为：
  - 索敌：检测视野范围内（500 像素）的玩家或敌对目标。
  - 寻路：使用简单 A* 寻路向目标移动，避开障碍物。
  - 攻击：当目标在射击角度（±15°）且距离合适时开火。
  - 闪避：当敌方炮弹靠近时（检测前方碰撞预测）尝试横向移动或后退。
  - 巡逻：无目标时随机移动。
- 胜利条件：消灭所有敌方坦克。
- 失败条件：玩家生命值归零。

#### 2.4.2 混战模式（Battle Royale）
- 所有坦克（包括玩家）各自为战，互相敌对。
- 初始生成 8~12 辆坦克（可在关卡设置中指定总数）。
- 玩家控制其中一辆，其余为 AI。
- 胜利条件：场上仅剩自己存活。
- 失败条件：玩家坦克被摧毁。
- AI 在混战中会攻击距离最近或仇恨最高的目标（谁打它它就优先打谁）。

### 2.5 坦克操控
- **移动**：W/S/A/D 或方向键。
- **射击**：鼠标左键（向鼠标光标方向发射炮弹）或 J 键/空格键（向坦克当前朝向发射）。
- **特殊武器切换**（若拾取临时弹药）：1/2/3 数字键切换当前弹药（如果有多种）。
- 鼠标控制炮塔朝向：炮塔始终跟随鼠标旋转（与车身朝向分离），玩家移动不影响炮塔朝向。即坦克车体可以向一个方向移动，炮塔独立旋转指向鼠标位置。
- 射界限制：无，可实现 360°射击。
- **窗口/全屏**：默认窗口化运行（视口见第 5 章），支持 F11 切换全屏。4K 全屏时视口逻辑尺寸不变，仅放大显示。

### 2.6 道具系统

#### 2.6.1 道具类型与效果
道具随机生成于地图空白区域，间隔 10~20 秒刷新一个，场上最多同时存在 3 个。拾取方式：坦克碰撞道具方块。

| 道具名称 | 图标    | 效果                                                                 | 持续时间 |
|----------|---------|----------------------------------------------------------------------|----------|
| 加速     | 闪电    | 移动速度 +50%                                                        | 8 秒     |
| 护盾     | 光盾    | 获得一个吸收 50 伤害的护盾                                           | 直到破碎 |
| 回血     | 十字    | 生命值回复 40 点（不超过最大值）                                      | 瞬间     |
| 导弹     | 导弹    | 下一发变为跟踪导弹，自动飞向最近敌方坦克，伤害 80，碰撞障碍物爆炸     | 1 发     |
| 核弹     | 辐射符号| 清屏炸弹：全屏敌方坦克受到 100 点伤害，有特效延迟                     | 瞬间     |
| 冰冻弹   | 雪花    | 下一发炮弹命中后使目标减速 60%，持续 3 秒                             | 1 发     |
| 散射弹   | 三连图标| 一次性向前方扇形发射 5 颗子弹                                         | 1 发     |
| 地雷     | 地雷图标| 在身后放置一个地雷，敌人触碰受到 50 伤害                              | 永久     |

- 道具外观可用不同颜色的发光方块或用户提供的图标。
- 拾取后播放音效和特效（坦克周身闪烁对应颜色光晕）。

#### 2.6.2 弹药系统扩展
- 玩家初始弹药为“普通弹”，无限数量，伤害 25。
- 拾取特殊弹药道具后，当前弹药切换为该类型，并显示在 HUD 上，剩余数量（例如导弹×1）。发射后切换回普通弹。
- 若连续拾取两个不同弹药道具，后者覆盖前者（不叠加），除非后者为一次性效果（核弹直接触发）。

### 2.7 碰撞与伤害
- 炮弹碰撞检测：
  - 与障碍物：子弹击中不可破坏物体消失，击中可破坏物体后物体消失（或损坏动画）子弹消失。
  - 与坦克：对敌方造成伤害（子弹消失），对我方（包括其他 AI 的炮弹）同样造成伤害。
- 坦克之间碰撞：不可重叠，硬碰撞（互相推挤或无法穿越）。
- 伤害计算：基础伤害固定值，不做距离衰减。
- 坦克死亡：播放爆炸动画，短暂留在地图上残留物，随后移除。
- 友军伤害：PVE 模式 AI 之间不会互相伤害（或可选项）；混战模式互相伤害。

### 2.8 用户界面与 HUD

#### 2.8.1 游戏画面布局（视口 1920×1080，4K 下缩放）
- 左上角：生命值条（红色渐变）、护盾条（蓝色）。
- 左下角：当前弹药图标和数量，特殊效果计时器（加速剩余秒数等）。
- 右上角：小地图。
- 顶部居中：关卡名称或“第 X 关”，剩余敌人数量。
- 准星：鼠标处显示十字准星。

#### 2.8.2 菜单界面
- 按钮使用圆角矩形 + 文本，悬停高亮，点击音效。
- 背景可动态展示坦克对战动画或静态背景图。
- 支持键盘和鼠标操作。

### 2.9 音频需求
- 背景音乐：菜单主题和战斗主题各一首，循环播放。
- 音效：开火、爆炸、拾取道具、护盾激活、核弹警告音、胜利/失败音。
- 所有音频文件可由用户提供，若缺失则跳过，不报错。

---

## 3. 非功能需求

### 3.1 性能
- 稳定 60 FPS（在 6000×6000 地图、10+ 坦克移动时）。
- 使用脏矩形或分块渲染优化，仅绘制屏幕可见区域。
- AI 寻路需异步或分帧计算，避免卡顿。

### 3.2 可扩展性
- 所有配置参数（坦克属性、道具、关卡）均存储在独立 JSON 或 CSV 文件中，方便非程序员修改。
- 图像/音效资源路径集中于配置文件，换皮容易。

### 3.3 错误处理
- 资源缺失时使用默认占位图形（纯色方块或简单形状），并输出警告日志，游戏不崩溃。
- 配置文件格式错误时提供清晰提示并退出。

### 3.4 跨平台
- 代码完全基于 Python + Pygame，无系统特定依赖。
- 键盘鼠标操作不依赖平台 API。

---

## 4. 技术实现指引

### 4.1 推荐目录结构
```
tank_battle/
├── main.py                 # 入口，初始化Pygame，启动游戏
├── settings.py             # 全局常量、颜色、路径
├── game.py                 # 游戏主循环、状态管理
├── sprites/
│   ├── tank.py             # 坦克基类及玩家、AI派生
│   ├── bullet.py           # 子弹、导弹、散射弹等
│   ├── powerup.py          # 道具类
│   ├── obstacle.py         # 障碍物（可破坏/不可破坏）
│   └── explosion.py        # 爆炸动画
├── systems/
│   ├── camera.py           # 镜头跟随
│   ├── minimap.py          # 小地图绘制
│   ├── ai.py               # AI状态机与寻路
│   └── collision.py        # 碰撞检测与处理
├── ui/
│   ├── menu.py             # 主菜单、模式选择等
│   ├── hud.py              # 游戏中HUD
│   └── popup.py            # 暂停、结算弹窗
├── levels/
│   ├── level_manager.py    # 加载关卡数据
│   └── levels.json         # 关卡配置文件
├── assets/
│   ├── images/             # 坦克、道具、障碍物、背景
│   ├── sounds/             # 音乐、音效
│   └── fonts/              # 字体
├── data/
│   ├── tank_data.json      # 坦克属性
│   └── powerup_data.json   # 道具定义
└── utils/
    ├── resource_loader.py  # 资源加载与缓存
    └── pathfinding.py      # A*算法实现
```

### 4.2 关键技术点
- **Pygame 精灵组**：使用 `pygame.sprite.Group` 管理坦克、子弹、道具、障碍物，方便碰撞检测。
- **地图绘制**：地图瓦片数据使用二维列表，绘制时仅绘制可见 Tile（结合 camera 偏移）。
- **小地图**：创建独立的 `pygame.Surface`，每帧清空后绘制缩放映射。
- **AI 寻路**：将地图栅格化为 64×64 的格子，标记可行走/不可行走，使用 A*。注意动态障碍物（其他坦克）可忽略或作为临时阻挡。
- **道具刷新**：使用 `pygame.time.set_timer` 自定义事件定时生成。
- **配置加载**：使用 `json` 模块读取，提供默认值以防缺失。
- **动画**：爆炸用精灵帧动画（一组图片或程序生成的渐变圆）。

### 4.3 主要类的设计简述
- `Game`：控制状态切换，主循环，事件分发。
- `Camera`：根据目标（玩家）计算偏移，传递给渲染函数。
- `Tank(pygame.sprite.Sprite)`：属性（hp, speed, ammo_type...），方法（move, shoot, take_damage）。`PlayerTank` 继承，添加输入处理。`AITank` 继承，添加状态机（patrol, chase, attack, evade）。
- `Bullet`：移动方向、速度、伤害值、所属坦克。
- `PowerUp`：类型、效果函数、生存时间超时自动消失。
- `Obstacle`：可破坏标记、耐久度（1 或正无穷）。
- `Explosion`：播放帧动画，结束后杀死自身。

### 4.4 AI 行为树/状态机简化版
```python
if target_in_range:
    if can_shoot:
        state = "attack"
    else:
        state = "chase"
else:
    if under_attack:
        state = "evade"
    else:
        state = "patrol"
```
每个 AI 坦克在 update 里根据状态调用 move, rotate_turret, shoot。

### 4.5 输入处理
- 连续按键使用 `pygame.key.get_pressed()` 实现平滑移动。
- 鼠标位置获取：`pygame.mouse.get_pos()` 转换为世界坐标用于炮塔朝向和射击方向。
- 事件：点击按钮、按 ESC 暂停。

---

## 5. 美术与资源规格

### 5.1 视口与缩放
- 游戏逻辑视口固定为 **1920×1080**。
- **4K 显示器（3840×2160）**：默认窗口化并以整倍数缩放显示；也可 F11 切换全屏。
- 地图 6000×6000 超出视口，镜头跟随玩家滚动。

### 5.2 资源规格（用户提供时需遵循）
- 图片格式：PNG（支持透明通道）。
- 坦克图：建议提供车身与炮塔分离的素材，以实现炮塔独立旋转；若提供单一图片，则整体旋转，炮塔固定向前。尺寸建议 48×48（可放大/缩小）。
- 障碍物图：64×64 像素，可重复平铺。
- 道具图标：32×32 像素。
- 小地图颜色：可配置透明度。

### 5.3 资源缺省替代方案（不提供资源时）
所有资源均可缺省，游戏以程序生成的几何图形 + 文字运行，不崩溃：

| 资源类别 | 缺省替代方案 |
|----------|--------------|
| 坦克图片 | 玩家绿色矩形+炮塔小矩形；敌方可按类型用不同颜色/形状矩形区分 |
| 障碍物 | 砖墙=棕色方块，钢墙=灰色方块，土墙=土黄色，木箱=木纹色，水=蓝色，草丛=绿色半透明方块 |
| 道具图标 | 带字母（S/P/H/M/N/F/S/D）的彩色发光方块 |
| 爆炸动画 | 程序生成渐变圆（从小到大再缩小消失） |
| 背景图 | 纯色或渐变色 |
| 音效/音乐 | 静音，功能不受影响 |

最低成本建议资源（仅 3 项即可让观感接近成品）：
1. 玩家坦克图（64×64，车身炮塔分离）
2. 敌方坦克图 2~3 种（不同颜色/形状区分类型）
3. 爆炸动画序列（或一张带帧的图像）

---

## 6. 交付与验收标准
- 程序双击 `main.py` 即可运行，弹出窗口标题为“坦克大战”。
- 主菜单所有按钮功能正常。
- PVE 关卡可正常加载配置，AI 具备基本智能，道具能刷新和生效。
- 混战模式能正常运行并决出胜者。
- 小地图正确反映战场动态。
- 游戏过程流畅无严重卡顿。
- 4K 显示器下画面缩放正确、镜头滚动顺滑。
- 所有异常资源缺失有默认处理，不崩溃。

---

## 7. 附录：示例配置文件

### `levels.json` 片段
```json
[
  {
    "id": 1,
    "name": "初次交锋",
    "map": { "width": 94, "height": 94, "tile_size": 64, "obstacles": "random", "density": 0.15 },
    "player_spawn": [3000, 5400],
    "enemies": [
      { "type": "light", "count": 3, "spawn_zone": "north" },
      { "type": "medium", "count": 2, "spawn_zone": "random" }
    ],
    "powerup_freq": 12,
    "max_powerups": 3,
    "mode": "pve"
  }
]
```

---

**文档版本历史**
- v2.0 – 针对 4K 显示器调整尺寸参数（地图 6000×6000、Tile 64×64、坦克 48~64、视口 1920×1080 缩放），补充资源缺省替代方案，明确阶段化开发（见《开发阶段拆分.md》）。
- v1.0 – 初始完整需求文档，适合 AI 编码工具实现。
