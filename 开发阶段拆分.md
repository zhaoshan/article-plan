# 坦克大战（Tank Battle）开发阶段拆分

**版本**：1.0
**配套文档**：《初始需求.md》（v2.0）
**目标**：将完整需求拆分为可独立交付、可运行、可验证的开发阶段，避免一次性实现陷入复杂陷阱，保证每个阶段都有可玩成果。

**核心原则**：每个阶段都以「可运行的游戏」为交付节点，而不是以「完成了某类函数」为节点。先打通核心可玩闭环，再叠加内容与细节。

**预计总工期**：约 3–4 周（单人 / 单 AI 编码工具环境）。

---

## 阶段总览

| 阶段 | 主题 | 交付物 | 预估工期 |
|------|------|--------|----------|
| **Phase 1** | 核心可玩 MVP | 单 PVE 关卡可玩闭环 | 1–2 周 |
| **Phase 2** | 功能完善版 | 道具 + 高级 AI + 多关卡 + 混战 | +1 周 |
| **Phase 3** | 完整版 | 全部高级道具 + Boss + 美化 + 性能优化 | +1 周 |

---

## 🟢 Phase 1 — 核心可玩 MVP

**目标**：一个能玩的 PVE 关卡，具备完整的「移动 → 战斗 → 胜负 → 结算」闭环。用几何图形替代全部美术资源。

### 交付物验收标准
- `python main.py` 可运行，窗口标题「坦克大战」
- 玩家可用 WASD 移动、鼠标瞄准、左键射击
- AI 敌人会索敌、追击、攻击
- 击毁全部敌人 → 胜利结算；玩家血量为 0 → 失败结算
- 镜头跟随玩家平滑滚动
- 小地图正确显示坦克与障碍物

### 本阶段范围

| 模块 | 包含内容 | 备注 |
|------|---------|------|
| 游戏框架 | 窗口初始化、主循环、状态机（菜单→游戏→暂停→结算） | 结构按《初始需求》4.1 |
| 坦克 | 玩家坦克：WASD + 鼠标瞄准 + 左键射击 | **炮塔固定向前，整体旋转** |
| 障碍物 | 不可破坏（砖墙/钢墙/水）+ 可破坏（土墙/木箱） | 地图 3000×3000 或直接 6000×6000 |
| 子弹 | 普通弹：移动、碰撞检测（坦克/障碍物） | |
| AI | 基础状态机：巡逻 → 索敌 → 追击 → 攻击 | **不用真 A*，直接朝目标移动，被障碍卡住时随机转向** |
| 地图 | 1 关，程序生成 | 随机障碍物布局 |
| HUD | 血量条、弹药类型、剩余敌人数 | |
| 小地图 | 220×220 半透明 | |
| 结算 | 胜利/失败弹窗 | |
| 资源 | 全部几何图形代替，无音效 | |

### 本阶段刻意不做
- 独立炮塔旋转（炮塔固定向前）
- 真正的 A* 寻路（用直冲+反弹代替）
- 所有道具系统
- 混战模式
- 多关卡（仅 1 关）
- 音频
- 图片素材

### 关键设计决策（为后续阶段留好接口）
- `Tank` 基类预留 `turret_angle` 字段和 `rotate_turret()` 方法，Phase 1 固定等于车身角度，Phase 2 改为跟随鼠标。
- `Sprites` 全部使用 `pygame.sprite.Sprite`，便于后续加碰撞组。
- AI 逻辑集中在 `systems/ai.py`，与寻路解耦，Phase 2 替换为 A* 时只改 `ai.py`。
- 地图尺寸、Tile、坦克属性全部读 `settings.py` 常量，Phase 2 起改为读 JSON 配置。

---

## 🔵 Phase 2 — 功能完善版

**目标**：加入道具系统、更智能的 AI（A* 寻路）、多关卡、混战模式。资源可选，几何图形仍可运行。

### 交付物验收标准
- 道具能随机刷新，拾取立即生效（加速/护盾/回血）
- AI 会绕开障碍物接近目标（A*），会闪避炮弹
- 关卡选择界面列出 ≥3 关，可切换
- 混战模式：8 辆坦克各自为战，决出唯一胜者
- ESC 暂停菜单（继续/重新开始/返回）

### 本阶段范围

| 模块 | 新增内容 | 备注 |
|------|---------|------|
| 道具系统 | **加速、护盾、回血** | 先不做导弹/散射弹/地雷/核弹/冰冻弹 |
| AI 升级 | A* 寻路（分帧计算）、闪避、瞄准精度参数 | |
| 关卡扩展 | 增加到 **5 关**，不同障碍物密度与敌人组合 | 关卡从 JSON 读取 |
| 混战模式 | 8 辆坦克各自为战 | AI 攻击距离最近目标（暂不实现仇恨表） |
| 炮塔分离 | 车身与炮塔分离独立旋转 | 需分离素材或程序绘制车身/炮塔两个图形 |
| 弹药系统 | 拾取特定道具后弹药切换，HUD 显示剩余数量 | |
| 音效 | 开火、爆炸、拾取道具音效 | 缺省静音 |
| 暂停菜单 | ESC 弹出，含继续/重新开始/返回主菜单 | |
| 配置 | 坦克属性、道具属性、关卡读 JSON | 拆出 `data/` 目录 |

### 本阶段刻意不做
- 高级道具（导弹、散射弹、地雷、核弹、冰冻弹）
- 仇恨值系统（AI 只打最近目标）
- 草丛视觉遮挡
- 10 个关卡（5 关即可）
- Boss

### 关键设计决策
- 道具用「类型标记 + 效果注册表」实现，追加新道具只需在 `powerup_data.json` 增加条目并注册一个效果函数。
- A* 放到独立线程或分帧（每帧只跑部分路径计算），避免卡顿。
- 混战与 PVE 共用 AI 逻辑，用不同的「目标选择策略」参数区分（最近 vs 指定玩家）。

---

## 🟠 Phase 3 — 完整版

**目标**：接近需求文档全部内容，追求成品观感、完整内容与 60FPS 稳定性能。

### 交付物验收标准
- 全部 8 种道具均可拾取生效（含跟踪导弹、核弹特效、散射弹、地雷、冰冻弹）
- 每关最后刷 Boss（重型强化版）
- 10 个关卡完整配置
- 草丛正确遮挡坦克（渲染层级处理）
- 菜单有动态背景、悬停高亮、点击音效
- 替换实际图片素材（若有）、背景音乐循环
- 6000×6000 地图、10+ 坦克下稳定 60 FPS

### 本阶段范围

| 模块 | 新增内容 | 备注 |
|------|---------|------|
| 高级道具 | 跟踪导弹、散射弹、地雷、核弹（全屏特效+延迟）、冰冻弹 | 逐一实现 |
| Boss | 每关最后刷 Boss（属性增强，初见为导弹弹种） | |
| 关卡 | 补齐到 10 关 | 难度曲线校准 |
| 草丛 | 视觉遮挡逻辑（按渲染层级绘制） | |
| 菜单美化 | 动态背景、悬停高亮动画、点击音效 | |
| 资源替换 | 用实际图片替换几何图形 | 用户提供或沿用占位 |
| 音频 | 菜单/战斗主题循环、核弹警告音、胜负音 | |
| 性能优化 | 视图裁剪、脏矩形、分帧寻路压测 | 确保 60FPS |
| 混战完善 | 仇恨值系统（谁打我打谁） | |

### 打磨项（本阶段并行）
- 爆炸与残留物动画细节
- 道具拾取光晕特效
- 关卡预览图（关卡选择界面）
- 死亡反馈（震颤、慢动作）

---

## 阶段间依赖与风险

### 依赖关系
- Phase 1 的 `Tank.turret_angle` 接口 → Phase 2 独立炮塔
- Phase 1 的精灵分组结构 → Phase 2 道具/混战
- Phase 2 的 A* + 目标选择策略 → Phase 3 仇恨值
- Phase 2 的 JSON 配置化 → Phase 3 10 关扩展

### 主要风险与对策
| 风险 | 影响阶段 | 对策 |
|------|---------|------|
| 6000×6000 地图 + 10 坦克 + A* 卡顿 | Phase 2–3 | 仅绘制可见 Tile；A* 分帧/独立线程；必要时降低 AI 更新频率 |
| 独立炮塔素材缺失 | Phase 2 | 用程序绘制「车身矩形+炮塔小矩形」两个 Sprite 拼接 |
| 跟踪导弹/核弹特效复杂 | Phase 3 | 延到最后，先用简单直线导弹与瞬发炸替代 |
| 4K 缩放坐标错位（鼠标/世界坐标换算） | Phase 1 | 视口逻辑尺寸固定 1920×1080，用统一缩放因子换算鼠标与世界坐标 |
| 10 关内容量大 | Phase 3 | 首版只保证 3 关精品，其余用随机生成填充 |

---

## 建议执行顺序（交付节奏）

1. **Phase 1** 单关可玩 → 立即停下交给用户试玩，收集手感反馈（移速、AI 强度、射速）再推进。
2. **Phase 2** 功能补齐 → 用户验收道具、寻路、混战是否达标。
3. **Phase 3** 内容与打磨 → 稳定后做全量验收。

> **强烈建议**每次完成一个阶段后都让玩家实际试玩一次，因为这类动作游戏的手感（移速、射速、AI 难度、碰撞判定）是纸上配置无法定准的，需要在实玩中反复校准。

---

**文档版本历史**
- v1.0 – 根据《初始需求.md》v2.0 拆分为 Phase 1/2/3 三阶段开发计划。
